#!/usr/bin/env python3
###############################
# (1) Define functions
#	(1.1) Main function to 
#	(1.2) 
# (2) Call function

# (1)

def gc_calculator (sequence):
	gc = 0
#	total = 0
	for n in range (0 len(seq))
#		if seq[n] in ['A','T','G','C']
#			total = total +1
		if seq[n] in ['G','C']:
			gc = gc+1
	gc_percentage = float(gc)/len(seq)*100

def max_gc (sequences)
	largest = sequences[0]
	for i in sequences:
		if i > largest
		largest = i
	return sequences.index(max)

file = open("/home/dylan/Documents/Other/Rosalind/Computing_GC_Content/test.txt")
data = file.read()
data = data.rstrip().split(' ')
id_list = list()
gc_list = list ()
long_seq=''
nowlen=1

for line in file.readlines():
	print (line)
	if (seq.startswith('>')):
		id_list.append(line[1:])
	#	id_list.append(line.lstrip('>'))
	else: 
		line=line.strip()
	#	line=line.rstrip('\n')
		

for line in file.readline():
	print (line)
	for n in line:
		if i == '>':
			id_list.append(line[1:])
			

def gc_calculator (sequence):
	def fasta_process (file_handle)
		seq_ID =  () # alternative None
		seq = []
		for line in file_handle
			line = line.rstrip()
			if i == '>':
				name

